name: mega_panel_ai
description: "Blueprint One wellness companion."
publish_to: 'none'
version: 1.0.1+51

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # Librerías necesarias para Mega Panel AI
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  google_generative_ai: ^0.4.6
  intl: ^0.19.0
  firebase_core: ^4.4.0
  cloud_firestore: ^6.1.2
  flutter_blue_plus: ^1.30.0
  flutter_local_notifications: ^19.4.2
  flutter_timezone: ^4.1.1
  permission_handler: ^11.3.0
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
